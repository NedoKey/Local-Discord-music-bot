# 🚀 Запуск MusicBot ULTRA

## Быстрый запуск

### Вариант 1: Запуск exe-файла (рекомендуется)

1. **Двойной клик** по файлу `start_bot_exe.bat`
   - Или запустите `dist\MusicBot_ULTRA.exe` напрямую

2. **Перед первым запуском:**
   - Создайте файл `token.txt` в папке `dist\` с токеном бота Discord
   - Папка `tracks` создастся автоматически

### Вариант 2: Запуск из Python скрипта

1. **Двойной клик** по файлу `start_bot_py.bat`
   - Или запустите: `py -3.11 musicbot_ultra.py`

2. **Перед первым запуском:**
   - Установите зависимости: `py -3.11 -m pip install -r requirements.txt`
   - Создайте файл `token.txt` с токеном бота Discord

### Вариант 3: Универсальный запуск

1. **Двойной клик** по файлу `start_bot.bat`
   - Автоматически определит, есть ли exe или нужно запускать через Python

## Файлы для запуска

- **start_bot.bat** - универсальный запуск (автоматически выбирает exe или Python)
- **start_bot_exe.bat** - запуск только exe-файла
- **start_bot_py.bat** - запуск только Python скрипта

## Требования

### Для exe-файла:
- Windows 10/11
- Файл `token.txt` с токеном бота

### Для Python скрипта:
- Python 3.11
- Все зависимости из `requirements.txt`
- Файл `token.txt` с токеном бота

## Настройка

1. **Токен бота:**
   - Создайте файл `token.txt` (рядом с exe или скриптом)
   - Вставьте токен бота Discord (без кавычек)

2. **Папки с музыкой:**
   - Откройте бота
   - Нажмите "🎵 Треки"
   - Нажмите "+ Папка" и выберите папку с `.mp3` файлами

3. **Голосовые каналы:**
   - Откройте бота
   - Нажмите "📋 Каналы"
   - Добавьте каналы с ID и именами

## Решение проблем

### Бот не запускается
- Проверьте, что `token.txt` существует и содержит токен
- Для Python: убедитесь, что все зависимости установлены

### Бот не подключается к каналу
- Проверьте ID канала в списке каналов
- Убедитесь, что бот имеет права на подключение к голосовым каналам
- Проверьте консоль на наличие ошибок

### Музыка не играет
- Проверьте, что в папках есть `.mp3` файлы
- Убедитесь, что бот подключен к голосовому каналу
- Проверьте громкость в настройках
